# main.py

from model1 import create_model1
from model2 import create_model2

# Assuming you've loaded data and prepared features
num_features = 100  # Adjust based on your data

# Create instances of the models
model1 = create_model1(num_features)
model2 = create_model2(num_features)

# Train and use the models


